generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Submission {
  id        String   @id @default(cuid())
  name      String   @unique
  slots     String   // JSON array of selected slot IDs
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
